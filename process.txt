Vars Needed
	//- v_init						--> -0.065f
	//- v_leak						--> Use the function get_v_leak
	- external_current_in	--> 0
	- memb_resista				--> 5621463123.21
	- v_leak_percent			--> 0.25

get_v_leak
	if(v_current > v_init)
		v_leak = v_leak_percent * abs(v_current)
	else
		return 0

get_gj_incoming
	multiply the proper value in the vector by the proper value in the gj matrix

get_cs_incoming
	multiply the proper value in the vector by the proper value in the cs matrix
total_incoming_current = gj_incoming + cs_incoming + external_current_in
total_incoming_voltage = memb_resist*total_incoming_current
v_current = (v_current - v_leak) + total_incoming_voltage
if(v_current < v_init)
	v_current = v_init
if(v_current > 0.15)
	v_current = 0.15


v_current, weight, gap_junction_conductance, polarity

                                                    incoming v_curr
 current = weight * gap_junction_conductance * (pre_syn_neuron_potential - v_current);


reversal_potential = -.065; // set to inhibit.. (was -0.045)
if (this.polarity == 1){
reversal_potential = 0; // V
}
double current = weight*chemical_synapse_conductance*(reversal_potential - v_current);


reversal_potential = -.065; // set to inhibit.. (was -0.045)
if (this.polarity == 1){
reversal_potential = 0; // V
}
double current = weight*chemical_synapse_conductance*(reversal_potential - v_current);
